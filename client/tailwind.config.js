/** @type {import('tailwindcss').Config} */
module.exports = {
    darkMode: 'class', // ✅ enable class-based dark mode
    content: ['./src/**/*.{html,js,svelte,ts}'], // ✅ scan all Svelte components
    theme: {
      extend: {},
    },
    plugins: [],
  };
  